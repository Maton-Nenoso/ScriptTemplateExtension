# ScriptTemplateExtension
A lightweight Unity Editor extension that adds Create context menu items for common C# file types beyond MonoBehaviour. C# Enum, C# Class, C# Abstract Class, C# Static Class, C# Interface, C# Struct, C# ScriptableObject.New templates can be added by simply dropping a .txt file into the templates folder — no code changes required.
